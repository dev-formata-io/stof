
#[hello]
/**
 * curl http://localhost:3030/hello -> greeting = "Hello, world!"
 * curl -d "name: 'John'" http://localhost:3030/hello -> greeting = "Hello, John!"
 */
fn greetings() {
    const name = Request.name ?? 'world';
    Response.greeting = `Hello, ${name}!`;
}
