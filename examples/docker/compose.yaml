# Generated with Stof
name: my-app
services:
  test_backend:
    build:
      context: backend
      target: builder
  test_proxy:
    depends_on:
    - backend
    image: nginx
    ports:
    - 80:80
    volumes:
    - read_only: true
      source: ./proxy/nginx.conf
      target: /etc/nginx/conf.d/default.conf
      type: bind
